<h1>Leave a review for "<%= @restaurant.name %>"</h1>

<div class="w-50">
  <%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :rating %>
    <%= f.input :content %>
    <%= f.submit %>
  <% end %>
</div>
